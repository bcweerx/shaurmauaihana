# Production cleanup — 09.09.2026

Checkpoint перед cleanup: **`de56ac7`**, `Checkpoint working React Vite site before production cleanup`. Первый коммит репозитория; автор `Codex <codex@localhost>` задан только для этого коммита, потому что пользовательская Git identity не была настроена. Глобальные настройки не менялись. Секреты, зависимости, сборки и исследовательские файлы в checkpoint не включены. Последующие изменения оставлены в рабочем дереве для просмотра.

## Изменённые файлы относительно checkpoint

| Файл | Изменение |
| --- | --- |
| `.gitignore` | Исключены зависимости, сборки, `.test-build`, `work`, локальные env, ключи и временные файлы; `.env.example` разрешён. Убраны остаточные правила Next/Yarn. |
| `.oxlintrc.json` | Исключён новый каталог тестовой сборки; удалены настройки исключений Next/Vinext. |
| `app/globals.css` | Исправлен порядок CSS-import и `body min-width`, создававший переполнение на 320 px при полосе прокрутки Windows. Палитра и дизайн сохранены. |
| `components.json` | `rsc: false` соответствует используемому React + Vite. |
| `lib/format.ts` | Стабильное отображение `грн` вместо зависящего от ICU символа: устранена ошибка React hydration #418 между Node (`₴`) и браузером (`грн`). Formatter переиспользуется. |
| `package.json` | Тестовая сборка перенесена из `work/tests` в `.test-build`; добавлена проверка результата build. Зависимости не менялись. |
| `scripts/prepare-site.mjs` | `SITE_URL` читается также из `.env` / `.env.local`; Open Graph type исправлен на `website`. |
| `tests/core.test.ts` | Тесты независимы от существования `dist`, проверяют SSR напрямую; добавлена регрессия формата валюты. |

## Добавленные файлы

- `.vercelignore` — исключение локальных материалов из CLI-upload.
- `scripts/check-build.mjs` — проверка HTML, metadata, запрета индексации, ассетов и изоляции результата сборки.
- `start-local.ps1` — запуск dev/preview с обычным Node или runtime Codex без автоматических установок.
- `README.md` — установка, запуск, данные, импорт меню, конфиг заказа, environment, SEO, подготовка Vercel.
- `docs/SOURCES.md` — источники и границы достоверности данных.
- `docs/CLEANUP.md` — этот отчёт.

## Удалённые файлы и каталоги

- `.npmrc` — неиспользуемая временная настройка `verify-deps-before-run=false`.
- `next-env.d.ts` — ссылки только на удалённый Vinext и `.next/types/routes.d.ts`; Vite/TypeScript его не использовали. Файл ранее был игнорируемым, поэтому удаление отсутствует в `git diff`.
- `.next/`, `.vinext/`, `.wrangler/` — старые генерируемые кеши предыдущего шаблона, не исходники.
- `work/tests/` — устаревший генерируемый результат тестов; новый путь `.test-build/`.
- `hooks/` — пустой каталог после ранее выполненного отделения шаблона.

Созданный во время диагностики `work/hydration.html` удалён после нахождения причины; временный диагностический сервер остановлен.

## Перенесено без удаления исходного содержания

Каждый из следующих файлов перенесён из `work/` в `work/research/`:

- `glovo-flight.txt`
- `glovo-legacy.html`
- `glovo.html`
- `import-server.mjs`
- `normalize-menu.py` (старая версия; рабочая версия — `scripts/normalize-menu.py`)

`work/starter-archive/` сохранён как локальный архив уже отделённого шаблона. Ни один файл из `work/` не требуется приложению, build или текущим скриптам импорта. `data/glovo-snapshot.json`, `data/menu-provenance.json`, `data/menu.json`, `data/menu.ts` и все фотографии сохранены. `.openai/hosting.json` содержит только статический target `dist`; backend-ресурсов нет.

## Результаты команд

Все четыре команды выполнены через pnpm 11.19.0 на итоговых исходниках:

| Команда | Результат |
| --- | --- |
| `pnpm typecheck` | PASS, exit 0 |
| `pnpm lint` | PASS, Oxlint, exit 0; диагностик нет |
| `pnpm test` | PASS, 8 тестов, 0 ошибок, exit 0 |
| `pnpm build` | PASS, exit 0; клиентская и статическая SSR-сборка, генерация SEO, check-build; предупреждений сборщика нет |

Финальный клиентский JS: 364.36 kB / gzip 108.04 kB; CSS: 47.90 kB / gzip 10.65 kB. Это размеры артефактов Vite, не оценка Lighthouse. Lighthouse и реальные iOS/Android-устройства на этом этапе не проверялись.

`git diff --check` — без ошибок. `git ls-files --cached --ignored --exclude-standard` — пустой результат. `git check-ignore` подтвердил исключения для node_modules, dist, .prerender, .test-build, work, .env*, ключей и временных файлов. `.env.example` остаётся доступным для Git. Проверка build не обнаружила служебных каталогов в `dist`.

## Browser QA

Проверена production-сборка через локальный Vite preview во встроенном Chromium. Размеры — CSS viewport; Windows занимает 15 px вертикальной полосой прокрутки.

| Viewport | clientWidth | scrollWidth | Переполнение |
| --- | --- | --- | --- |
| 320 | 305 | 305 | Нет |
| 375 | 360 | 360 | Нет |
| 390 | 375 | 375 | Нет |
| 430 | 415 | 415 | Нет |
| 768 | 753 | 753 | Нет |
| 1024 | 1009 | 1009 | Нет |
| 1440 | 1425 | 1425 | Нет |
| 1920 | 1905 | 1905 | Нет |

Проверены открытие мобильной навигации, переход к меню и закрытие панели, добавление блюда, изменение количества и суммы, копирование списка, сохранение после reload, удаление позиции и очистка корзины. Escape закрывает корзину и возвращает фокус на кнопку открытия. Корзина проверена на 320/375/390/430/768/1440 px без внутреннего горизонтального переполнения; её основные кнопки имеют размеры не меньше 44 px.

Проверены фильтр напитков (11 позиций), пустой поиск, возврат ко всему меню и последовательное раскрытие всех 117 карточек. Все внутренние якоря разрешаются. У загруженных в браузере изображений ошибок нет; существование всех 116 локальных фотографий дополнительно проверено тестом. Одна позиция использует предусмотренную заглушку без фото.

После исправления валюты новая загрузка и дальнейшие действия не дали console errors/warnings. Обнаруженная ранее React #418 устранена, а не скрыта обработчиком ошибок.

## Состояние публикации

React + Vite, архитектура, цветовая схема и hero сохранены. Backend, БД, авторизация, платёжный код и Telegram-бот не добавлены. `ownerVerified: false`, `indexable: false`, `orderChannel: "none"` сохранены. В HTML `noindex,nofollow`, в robots — `Disallow: /`. Без реального домена canonical и sitemap не создаются.

Технические приготовления к preview deployment завершены; сам Vercel deployment не выполнялся. Для рабочего приёма заказов нужны подтверждённый телефон/username и выбранный канал. Перед публичной коммерческой версией владелец должен подтвердить часы, адрес, цены и спорные описания, использование фотографий; затем можно подключить домен. Индексация включается отдельно после подтверждения данных.
